[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "auxilab-agent-payment-run"
version = "0.1.0"
description = "Agentic AI · Pre-payment batch validation and CFO-ready authorisation summary generation"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
keywords = ["agentic-ai", "mcp", "finance", "ap-automation", "auxiliobits"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

[project.urls]
Homepage = "https://github.com/AuxiLabs-Auxiliobits/auxilab-agent-payment-run"
Organisation = "https://github.com/AuxiLabs-Auxiliobits"
Website = "https://auxiliobits.com/auxilab"
